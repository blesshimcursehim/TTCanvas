{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "player",
  "description": "Capability for the player window",
  "windows": ["player"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "log:default"
  ]
}
