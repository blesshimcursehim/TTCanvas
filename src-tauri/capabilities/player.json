{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "player",
  "description": "Capability for the player window",
  "windows": ["player"],
  "permissions": [
    "core:default",
    "log:default"
  ]
}
